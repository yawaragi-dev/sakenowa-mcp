node_modules/
dist/
*.log
.DS_Store
.env*

# Claude Code / agent workflow tooling — local dev only, not part of the
# published package (see package.json "files"). The sister Yawaragi repo tracks
# .claude/skills + .claudeignore + skills-lock.json; mirror that here if you
# want cross-repo uniformity.
.claude/
.agents/
.claudeignore
skills-lock.json
